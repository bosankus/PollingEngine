#Kotlin
kotlin.code.style=official
kotlin.daemon.jvmargs=-Xmx3072M

#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true

#Android
android.nonTransitiveRClass=true
android.useAndroidX=true

# --- Maven Central (Sonatype) publishing configuration ---
# Set these in ~/.gradle/gradle.properties or as environment variables in CI
# ossrhUsername=your_sonatype_username
# ossrhPassword=your_sonatype_password
# signing.keyId=XXXXXXXXXXXXXXXX
# signing.password=your_gpg_key_passphrase
# signing.key=-----BEGIN PGP PRIVATE KEY BLOCK-----
# (armored private key contents)
# -----END PGP PRIVATE KEY BLOCK-----
# Optionally project-wide coordinates (also set in module build.gradle.kts)
# GROUP=in.androidplay
# VERSION_NAME=0.1.0
